// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // images: {
  //   domains: ['themes12.anvanto.com'],
  // },
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
